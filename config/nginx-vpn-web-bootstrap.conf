server {
    listen 80;
    listen [::]:80;
    server_name __VPN_WEB_DOMAIN__;

    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }

    location / {
        return 503;
    }
}
