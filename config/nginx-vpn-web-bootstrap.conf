server {
    listen 80;
    listen [::]:80;
    server_name vpn.example.com;

    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }

    location / {
        return 503;
    }
}
