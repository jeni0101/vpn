# Tokyo is an IPv4 exit. IPv6 enters the tunnel and is explicitly
# rejected by nftables so a client can never fall back to its local IPv6 route.
net.ipv4.ip_forward = 1
net.ipv6.conf.all.forwarding = 1
